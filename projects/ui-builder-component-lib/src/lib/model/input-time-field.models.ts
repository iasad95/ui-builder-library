export interface TimeSlot {
  slotLabel: string;
  slotLabelWitHours?: string;
  slotTime?: number;
  slotValue: Date;
}
export interface TimeSlotHours {
  slotLabelWitHours: string;
  slotTime: number;
}
